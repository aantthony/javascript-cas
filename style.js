(function (arguments, other){
	// Tab size: 4 (not spaces)
	var j = 2,
		k,
		l;
	if (statement) {
		// Variable declared in block where it will be used
		// But don't use this variable (or one of the same name)
		// after the if statement.
		var i;
		for (i = 0, l = 3; i < l; i++) {
			arr.push(i);
		}
	} else if (other) {
		return function (x) {
			return 2 * x;
		};
	}
	return "1/x";
}());
